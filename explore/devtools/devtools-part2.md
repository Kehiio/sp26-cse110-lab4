1. The bug was that the inputed numbers were being added as strings instead of numbers.
2. I would solve this by explicitly converting both inputs to numbers in calculateSum() before adding them, checking that the data type is correct, and flagging if the results were not numbers. 
